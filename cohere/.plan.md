# Cohere on Microsoft Foundry — 3‑Hour Workshop: Reusable Implementation Plan

> This plan is the **authoritative spec** for the workshop that lives at `cohere/` in this
> repository. It is written so that a different agent, on a different device or Azure
> account, can re-implement the workshop end-to-end from scratch and arrive at the same
> shape, conventions, and behaviour. Every section captures both the **what** and the
> **why** so the next implementer can deviate intelligently when needed.

---

## 1. Workshop goals and audience

- **Duration**: 3 hours, self-guided.
- **Audience**: technical (L200–L400). Assume strong programming skills but **new** to
  Microsoft Foundry, Cohere models, embeddings, rerank, AI agents, and AI evaluation.
- **Outcome**: by the end the learner can (1) deploy Cohere models on Microsoft Foundry,
  (2) build, evaluate, red-team and monitor an agentic app with Command A Plus Plus, and (3)
  choose the right Cohere model for the right task (embed / rerank / multimodal).
- **Story**: **enterprise travel concierge** — books cars/flights/hotels and honours a
  corporate travel policy. The story runs through Lab 1, with Lab 0 setting up the
  factory and Lab 2 deep-diving into Cohere's other specialised models.

## 2. Naming and product conventions (must follow)

- The product is **Microsoft Foundry**. Never write "Azure AI Foundry".
- Prefer the **new Foundry SDK** (`azure-ai-projects`) wherever possible. Fall back to
  the Cohere SDK only when needed (e.g., `cohere.ClientV2` for the upstream Cohere
  notebooks that rely on its native call shapes).
- Auth: prefer `DefaultAzureCredential` (AAD). Account API keys are exposed as env
  vars only because the upstream vendor notebooks use them.
- The folder lives at `cohere/`. Sibling provider folders (`anthropic/`, `foundry/`) are
  separate workshops outside this plan's scope.

## 3. Authoritative model deployment facts

The four model deployments learners create (and which the reference Foundry project
`nitya-modelmastery-cohere` in `eastus2` already has) are:

| Deployment name           | Catalog model            | Version | Format | sku.name        | sku.capacity |
| ------------------------- | ------------------------ | ------- | ------ | --------------- | ------------ |
| `text-embedding-3-small`  | `text-embedding-3-small` | `1`     | OpenAI | `GlobalStandard`| 120          |
| `command-a-plus`               | `Cohere-command-a-plus-05-2026`       | `1`     | Cohere | `GlobalStandard`| 1            |
| `Cohere-rerank-v4.0-pro`  | `Cohere-rerank-v4.0-pro` | `1`     | Cohere | `GlobalStandard`| 1            |
| `embed-v-4-0`             | `embed-v-4-0`            | `1`     | Cohere | `GlobalStandard`| 1            |

Foundry resource shapes:
- Foundry account = `Microsoft.CognitiveServices/accounts` with `kind=AIServices`,
  `sku.name=S0`.
- Foundry project = `Microsoft.CognitiveServices/accounts/projects` child resource.
- Account endpoint pattern: `https://<account>.services.ai.azure.com/`.
- Project endpoint pattern (for `azure-ai-projects`):
  `https://<account>.services.ai.azure.com/api/projects/<project>`.
- Cohere ClientV2 `base_url` pattern: `https://<account>.services.ai.azure.com/providers/cohere`
  (ClientV2 auto-appends `/v2/embed` or `/v2/rerank`).

Region default: **eastus2** (validated to host all four models above).

## 4. Decisions locked in (with rationale)

| # | Decision | Rationale |
| --- | --- | --- |
| 1 | Lab 0 provisioning = **Bicep template + deploy script** | Declarative, repeatable, learners can read it; portal flow is a one-paragraph appendix |
| 2 | Lab 1 tools = **mocked Python functions** | Simplest agent-tools demo; no extra Azure deployments |
| 3 | Lab 1 red-team = **cloud red-team scan via AI Red Teaming agent** | Live demo; uses latest SDK |
| 4 | Lab 1 load test = **asyncio + httpx, configurable RPS** | No extra install beyond requirements.txt |
| 5 | Lab 2 vendor notebooks = **copy & sanitize, keep intact** | Vendor content stays intact; customization isolated to capstone |
| 6 | Lab 2 packaging = **5 vendor notebooks + 1 capstone sandbox** | `99-try-it-yourself-travel.ipynb` is the only place we customize / play |
| 7 | Setup = **GitHub Codespaces flow primary**, local secondary | Devcontainer auto-installs deps; `az login --use-device-code` for Codespaces |
| 8 | Notebook style = strict (see §6) | Every notebook tells the same shape of story |
| 9 | Voice = high-school reading level, define terms on first use | Audience is technical but new to AI concepts |
| 10 | Docs use **Mermaid diagrams** for complex flows | Visual learners; renders inline on GitHub + VS Code |
| 11 | Analogies = **car-factory (Lab 0)** + **travel-concierge (Labs 1 & capstone)** | Two grounded analogies, used consistently |
| 12 | No time allocation table | Each notebook carries its own `⏱` badge |

## 5. Final folder layout (mirror exactly)

```
cohere/
├── README.md                       # workshop overview, story, get-started (Codespaces-first), conventions
├── requirements.txt                # grouped + commented deps; chained from repo-root requirements.txt
├── sample.env                      # canonical env-var reference; setup-env.sh copies → .env
├── lab-0-setup/
│   ├── SETUP.md                    # Scenario(car-factory) → Codespaces flow → local alt → 2 Mermaid diagrams
│   ├── bicep/
│   │   ├── main.bicep              # RG-scope: Foundry account + project + 4 deployments + LogAnalytics + AppInsights
│   │   └── main.parameters.json    # deployments array + names matching §3
│   ├── provision.sh                # az deployment group create + post-checks
│   ├── setup-env.sh                # prompts for RG; copies sample.env→.env; populates all vars via az CLI
│   └── verify.sh                   # lists deployments + pings each (chat, embed, rerank)
├── lab-1-foundry-agent/
│   ├── README.md                   # lab guide + concierge system overview + hill-climb loop diagrams
│   ├── _tracing.py                 # configure_azure_monitor wrapper; no-op when APPLICATIONINSIGHTS_CONNECTION_STRING unset
│   ├── 01-verify-model.ipynb       # confirm command-a-plus reachable via azure-ai-projects (+ Cohere SDK fallback)
│   ├── 02-build-agent.ipynb        # create prompt agent travel-concierge backed by command-a + register tools + grounding
│   ├── 03-test-prompts.ipynb       # 8–10 smoke prompts
│   ├── 04-eval-dataset.jsonl       # 20 rows: query / expected_intent / axis ∈ {quality,safety,agentic,policy}
│   ├── 05-eval-round1-baseline.ipynb # built-in evaluators (Groundedness, Relevance, Coherence, Fluency, ContentSafety, IntentResolution, ToolCallAccuracy, TaskAdherence) — baseline generic instructions
│   ├── 06-eval-round2-grounded.ipynb # tighter instructions + grounding data; rerun + show delta
│   ├── 07-eval-round3-policy-evaluator.ipynb # add custom PolicyAdherenceEvaluator
│   ├── 08-redteam-scan.ipynb       # AI Red Teaming agent cloud scan
│   ├── 09-load-test.py             # asyncio + httpx; CLI: --rps --duration --failure-ratio
│   ├── 10-monitoring-walkthrough.md# Monitoring tab guide tied to load-test output
│   ├── bridge/
│   │   └── travel-capability-hooks.md # 5 "if only my concierge could…" prompts mapped to Lab 2 notebooks
│   ├── data/
│   │   ├── travel-policy.md        # ~1 page synthetic corporate travel policy (numbered rules)
│   │   └── catalogs/
│   │       ├── flights.json
│   │       ├── hotels.json
│   │       └── cars.json
│   ├── evaluators/
│   │   └── policy_adherence.py     # custom evaluator: LLM-as-judge using Command A Plus → returns violates / score / reasoning
│   └── tools/
│       └── booking_tools.py        # search_/book_ functions for flights, hotels, cars
└── lab-2-cohere-capabilities/
    ├── README.md                   # bridge from Lab 1 + capability map (Mermaid) + endpoint URLs + tips + attribution
    ├── 2a-embed.ipynb              # vendor: text embeddings introduction (sanitized, workshop-framed)
    ├── 2b-embed.ipynb              # vendor: multimodal business-graph search; index + 3 search modes (image, text, image+text)
    ├── 2c-rerank-getting-started.ipynb # vendor: semantic reranking introduction
    ├── 2d-rerank-structured-data.ipynb # vendor: rerank over YAML-serialised structured data
    ├── 2e-rerank-wikipedia-search.ipynb # vendor: rerank Wikipedia lexical results
    ├── 99-try-it-yourself-travel.ipynb # capstone sandbox: embed/graphs/rerank/chart-understanding applied to travel + lifted "From 2x" sandbox sub-sections
    ├── chroma_db/                  # vendor naming; populated locally by 2b run
    ├── dataset/                    # vendor business-graph images (graph_*.jpg)
    ├── dataset.test/               # vendor 4-image test subset
    ├── search_images/              # vendor sample query images
    └── try.travel/
        ├── corpus/                 # ~15–25 travel text snippets for capstone embedding/rerank
        └── charts.html             # standalone HTML chart figure for capstone multimodal exercise
```

## 6. Notebook style conventions (mandatory, every notebook)

Every notebook (including the vendor 2a–2e) must conform to this outer frame, with
only minimal sanitization of the vendor body:

1. A one-line **time-estimate badge** at the very top, e.g. `> ⏱ ~12 min`.
2. **Section 1** = `# Scenario` markdown that sets the stage.
3. **Section 2** = `## 2. What you will do` numbered task list, **followed by a `---`
   horizontal rule** so it is visually clear where the lab body begins.
4. Body sections numbered sequentially. Alternate markdown and code cells; markdown
   teaches intent and gives analogies, code carries meaningful inline comments.
5. **Final section** = `## N. What you learned` with **exactly 3 takeaways**.

Additional voice rules:
- Write for a high-school reading level. Short sentences, common words, define
  terms on first use in each file (embedding, rerank, evaluator, prompt agent,
  deployment, telemetry, red-team, RPS, App Insights, Foundry project, …).
- Use the established analogies — car-factory for Foundry, travel-concierge for the
  Lab 1 agent. Do **not** add new travel analogies inside the vendor 2a–2e bodies.
- Code comments teach intent, not syntax (`# We poll every 5 s because the SDK
  doesn't surface a push subscription yet`, not `# increment i`).

## 7. Cross-cutting docs and visuals (Mermaid diagrams to include)

Use `flowchart LR` or `flowchart TB` only. Cap at ~10 nodes per diagram.

| Doc | Diagrams |
| --- | --- |
| `cohere/README.md` | Workshop arc: Lab 0 → Lab 1 → Lab 2 → Capstone, with one-line value per lab |
| `cohere/lab-0-setup/SETUP.md` | (1) Car-factory topology of Foundry account + project + 4 model engines + AppInsights camera; (2) provisioning happy path `az login → provision.sh → setup-env.sh → verify.sh` with a parallel branch for pre-provisioned RG |
| `cohere/lab-1-foundry-agent/README.md` | (1) Concierge system overview: User → Agent (Command A Plus) → tools + grounding + custom evaluator; OpenTelemetry → App Insights → Foundry Monitoring tab; (2) Build → evaluate → iterate hill-climb across notebooks 02–09 |
| `cohere/lab-2-cohere-capabilities/README.md` | Capability map: Lab 1 concierge → bridge hooks → Embed (2a, 2b) / Rerank (2c, 2d, 2e) / Capstone (99) with one-line analogies |

## 8. Dependencies (`cohere/requirements.txt`)

Organised into commented groups so the file doubles as orientation. Codespaces installs
it automatically via `.devcontainer/post-create.sh` → root `requirements.txt` which
chains `-r cohere/requirements.txt`.

Groups + canonical pins:
- **Microsoft Foundry / Azure SDKs**: `azure-ai-projects>=2.2.0` (v2 preview for prompt-agent
  `create_version` + typed `PromptAgentDefinition` / `FunctionTool`), `azure-ai-evaluation>=1.0.0`,
  `azure-identity>=1.17`, `azure-monitor-opentelemetry>=1.6`.
- **Cohere SDK**: `cohere>=5.0`.
- **Vector store**: `chromadb>=0.6`.
- **Data + viz**: `pandas>=2.0`, `numpy>=1.24`, `matplotlib>=3.0`, `seaborn>=0.12`, `pillow>=10.0`.
- **Notebook tooling**: `ipykernel>=6.0`, `ipywidgets>=8.0`, `tqdm>=4.0`, `python-dotenv>=1.0`, `PyYAML>=6.0`.
- **Demo data sources**: `wikipedia-api>=0.6`, `requests>=2.28`.
- **Tracing / OpenTelemetry**: `opentelemetry-api>=1.27`, `opentelemetry-sdk>=1.27`, `opentelemetry-instrumentation-httpx>=0.48b0`.
- **Load testing**: `httpx>=0.27`, `anyio>=4.0`.

## 9. Environment variables (`cohere/sample.env`)

Canonical list. `setup-env.sh` copies this to `.env` and fills concrete values from
the resource group via Azure CLI.

| Group | Variable | Notes |
| --- | --- | --- |
| Azure identity | `AZURE_SUBSCRIPTION_ID` | `az account show --query id` |
| | `AZURE_TENANT_ID` | `az account show --query tenantId` |
| | `AZURE_LOCATION` | default `eastus2` |
| Lab 0 resources | `AZURE_RESOURCE_GROUP` | user-provided |
| | `FOUNDRY_ACCOUNT_NAME` | the AIServices account in the RG |
| | `FOUNDRY_PROJECT_NAME` | the child project on the account |
| | `AZURE_AI_ENDPOINT` | `https://<account>.services.ai.azure.com/` |
| | `FOUNDRY_PROJECT_ENDPOINT` | `<AZURE_AI_ENDPOINT>api/projects/<project>` |
| Lab 0 deployments | `COMMAND_A_PLUS_DEPLOYMENT=command-a` | lookup by `properties.model.name=Cohere-command-a-plus-05-2026` so pre-provisioned RGs with different casing still work |
| | `EMBED_V4_DEPLOYMENT=embed-v-4-0` | lookup by `embed-v-4-0` |
| | `RERANK_DEPLOYMENT=Cohere-rerank-v4.0-pro` | lookup by `Cohere-rerank-v4.0-pro` |
| | `OPENAI_EMBED_DEPLOYMENT=text-embedding-3-small` | lookup by `text-embedding-3-small` |
| | `AZURE_OPENAI_API_VERSION=2024-10-21` | |
| | `AZURE_INFERENCE_API_VERSION=2024-05-01-preview` | |
| Lab 1 agent + ops | `LAB1_AGENT_ID` | written back by notebook 02 |
| | `LAB1_AGENT_NAME=travel-concierge` | |
| | `APPLICATIONINSIGHTS_CONNECTION_STRING` | from the provisioned AppInsights component |
| | `LAB1_LOADTEST_RPS=2` | load-test defaults |
| | `LAB1_LOADTEST_DURATION_S=120` | |
| | `LAB1_LOADTEST_FAILURE_RATIO=0.15` | |
| Lab 2 — vendor (Cohere ClientV2) | `EMBED_BASE_URL` | `<AZURE_AI_ENDPOINT>providers/cohere` |
| | `EMBED_API_KEY` | account key 1 |
| | `EMBED_MODEL=embed-v-4-0` | vendor-parity alias for `EMBED_V4_DEPLOYMENT` so upstream `cohere-azure-workshops` notebooks run unmodified |
| | `RERANK_BASE_URL` | same pattern as above |
| | `RERANK_API_KEY` | account key 1 |
| | `RERANK_MODEL=Cohere-rerank-v4.0-pro` | vendor-parity alias for `RERANK_DEPLOYMENT` |
| | `CHROMA_DB_PATH=./chroma_db` | vendor naming |
| Lab 2 — local | `LAB2_CHROMA_DIR=./lab-2-cohere-capabilities/.chroma` | new-style; used by the capstone |

## 10. Lab-by-lab implementation guide

### Lab 0 — Setup

**Bicep (`bicep/main.bicep`)** — `targetScope = 'resourceGroup'`. Parameters: `location`
(default `eastus2`), `foundryAccountName`, `foundryProjectName`, `appInsightsName`,
`logAnalyticsName`, `deployments` (array). Resources: AIServices account (api version
`2024-10-01`), project child (`2025-04-01-preview`), `@batchSize(1)` model deployments,
Log Analytics workspace (PerGB2018, 30-day retention), workspace-based Application
Insights. Outputs: `foundryAccountName`, `foundryProjectName`, `foundryEndpoint`,
`projectEndpoint` (= `'${account.properties.endpoint}api/projects/${project.name}'`,
account endpoint already ends with `/`), `appInsightsConnectionString`.

**`main.parameters.json`** — sensible defaults (e.g. `mm-cohere-workshop`,
`mm-cohere-project`, `mm-cohere-appi`, `mm-cohere-la`) plus the four deployments array
matching §3.

**`provision.sh`** — bash, `set -euo pipefail`. Reads/prompts `RESOURCE_GROUP`,
`LOCATION` default `eastus2`. `az group create`, `az deployment group create`. Prints
outputs. Idempotent.

**`setup-env.sh`** — bash, `set -euo pipefail`. Args: `[--force] [RESOURCE_GROUP]`.
Refuses to overwrite `.env` unless `--force`. Looks up subscription id, tenant id,
AIServices account in RG, child project, deployments **by `properties.model.name`**
(so pre-provisioned RGs with different deployment casing still work), App Insights
component name + connection string, and **Foundry account key 1** for the Cohere
ClientV2 vendor env vars. Computes `COHERE_BASE_URL = ${AZURE_AI_ENDPOINT%/}/providers/cohere`.
Copies `sample.env` → `.env` then sed-replaces every variable. Prints a summary table
(mask the API key as `[set]`).

**`verify.sh`** — sources `../.env`. Lists deployments, then makes one inference call
per deployment using `az account get-access-token --resource https://cognitiveservices.azure.com`
and `curl`. Prints PASS/FAIL per deployment.

**`SETUP.md`** — uses the **car-factory analogy** (Foundry = factory, model = engine,
agent = chassis, instructions = manual). Leads with the GitHub Codespaces flow
(devcontainer auto-installs deps; `az login --use-device-code` because Codespaces
cannot open a local browser). Local-machine path is presented as the alternative.
Two Mermaid diagrams: factory topology + provisioning happy path with pre-provisioned
RG branch.

### Lab 1 — Travel concierge with Command A Plus

**Tools (`tools/booking_tools.py`)** — pure-Python functions: `search_flights`,
`search_hotels`, `search_cars` (each with a `book_*` partner). Read from
`data/catalogs/*.json`, filter reasonably, return list[dict]. Docstrings are detailed
because they are read by the agent as tool descriptions.

**Data**
- `data/travel-policy.md` — ~1 page synthetic policy, **numbered rules** so the
  custom evaluator can reason about specific rule violations.
- `data/catalogs/{flights,hotels,cars}.json` — 10–20 records each with fields the
  tools filter on. **Date convention:** all `depart_date`, `return_date`,
  `available_from`, `available_to` values fall in **July–December 2026** to keep
  the synthetic data future-dated; the `04-eval-dataset.jsonl` queries reference
  the same window. Re-spread these dates when re-running the workshop in a future
  year so prompts stay future-dated relative to the run date.

**Custom evaluator (`evaluators/policy_adherence.py`)** — class implementing the
`azure-ai-evaluation` custom-evaluator protocol (`__call__(*, query, response, **kwargs)
-> dict`). Uses **Command A Plus as the judge model** via the OpenAI-compatible client
exposed by `azure-ai-projects` against the Foundry deployment. Returns three keys:
`violates_policy: bool`, `policy_adherence_score: int` (0–5), `reasoning: str`.
Gracefully degrades when `FOUNDRY_PROJECT_ENDPOINT` is unset.

**Tracing helper (`_tracing.py`)** — `configure_tracing()` calls
`azure.monitor.opentelemetry.configure_azure_monitor` using
`APPLICATIONINSIGHTS_CONNECTION_STRING` and instruments httpx. Safe no-op when env
var is unset.

**Notebooks**
1. `01-verify-model.ipynb` — `AIProjectClient.deployments.list()` to confirm
   `command-a-plus` exists; one chat completion via the project's OpenAI-compatible client;
   brief Cohere SDK fallback example.
2. `02-build-agent.ipynb` — create a Foundry **prompt agent** named `travel-concierge`
   backed by `command-a-plus`. Register the booking tools (function-calling). Attach
   `data/travel-policy.md` as grounding (vector store if SDK supports it, else
   inlined into the system prompt — document both). Persist `LAB1_AGENT_ID` back
   into `.env`.
3. `03-test-prompts.ipynb` — 8–10 smoke prompts (one is intentionally a luxury-car
   request to provoke a policy nudge).
4. `04-eval-dataset.jsonl` — 20 rows, fields `{query, expected_intent, axis}`,
   covering quality / safety / agentic / policy.
5. `05-eval-round1-baseline.ipynb` — built-in evaluators (Groundedness, Relevance,
   Coherence, Fluency, ContentSafety, IntentResolution, ToolCallAccuracy,
   TaskAdherence) against baseline generic instructions. Upload to Foundry; print
   summary table + portal-URL pattern.
6. `06-eval-round2-grounded.ipynb` — tighten instructions + attach catalogs +
   policy as grounding; rerun; show delta vs round 1.
7. `07-eval-round3-policy-evaluator.ipynb` — add `PolicyAdherenceEvaluator`; rerun;
   show what it catches that the built-ins missed.
8. `08-redteam-scan.ipynb` — cloud red-team scan via the AI Red Teaming agent in the
   latest SDK. Graceful degradation when `azure-ai-evaluation[redteam]` is not
   installed.
9. `09-load-test.py` — asyncio + httpx. CLI `--rps --duration --failure-ratio`,
   env-overridable. Reads `FOUNDRY_PROJECT_ENDPOINT` + `LAB1_AGENT_ID`. Posts mixed
   prompts (eval-dataset + intentionally malformed ones for failures). Exits with
   `total / success / 4xx / 5xx / p50 / p95`.
10. `10-monitoring-walkthrough.md` — guides the learner through the Foundry
    Monitoring tab after running the load test.

**Bridge (`bridge/travel-capability-hooks.md`)** — five travel "if only my concierge
could…" prompts each mapped to a Lab 2 notebook (2a, 2b, 2c, 2d, 2e).

**README** — concierge system overview + hill-climb-loop Mermaid diagrams.

### Lab 2 — Cohere capabilities

**Source of truth (vendor)**: `https://github.com/cohere-ai/cohere-azure-workshops`.
The original local copy used during this implementation lived at
`.do-not-commit/lab2-cohere-focused/`.

**Policy**: **Keep the 2a–2e content as close to vendor intact as possible.** The only
allowed modifications:
1. Add the workshop frame (time badge, Scenario, What you will do + `---`, What you
   learned with 3 takeaways).
2. Replace hard-coded secrets/paths with `os.environ` reads using the names in §9.
3. Route Cohere SDK calls through the Foundry deployment (Cohere ClientV2 with
   `base_url=...providers/cohere`) instead of the public Cohere API.
4. Repath absolute `.do-not-commit/` references to relative paths.
5. Drop stale outputs that contain secret-looking strings.

Do **not** add travel analogies inside 2a–2e bodies. Add a single cross-link
markdown cell at the end of each (just before `What you learned`) pointing to the
matching `From 2x` sub-section in the capstone.

**Capstone (`99-try-it-yourself-travel.ipynb`)** is the only place we customize. It
contains a `## N. Sandbox: per-capability travel experiments` section with five
labeled sub-sections (`### From 2a-embed`, `### From 2b-embed`, etc.), each lifting
the travel exercise that used to live in the corresponding focused notebook and
applying it to the travel scenario from Lab 1.

**Data folders (vendor naming preserved)**:
- `dataset/` — full business-graph image set.
- `dataset.test/` — 4-image quick test subset.
- `search_images/` — sample query images.
- `chroma_db/` — empty placeholder (`.gitkeep`); notebooks populate it locally.
- `try.travel/corpus/` + `try.travel/charts.html` — travel customisation data used
  only by the capstone (and the capstone's sandbox sub-sections).

**README** — bridge from Lab 1, capability-map Mermaid, "Cohere on Foundry endpoint
URLs" subsection (explaining `/providers/cohere` and ClientV2 auto-append), Tips
about opening each notebook from its folder so relative paths resolve, and an
attribution footer linking the upstream Cohere repo.

## 11. Implementation order (todos)

Run in this sequence (parallelism in parentheses):

1. **Foundation (sync, do yourself)** — folder skeleton; `cohere/README.md`;
   `cohere/sample.env`; rewrite `cohere/requirements.txt` into commented groups;
   install deps and confirm they resolve.
2. **Three parallel agents** — Lab 0, Lab 1, Lab 2 each as its own background
   agent. They are independent because the foundation is in place.
3. **Sequential follow-ups (each gated on its predecessors)**:
   - Enrich 2b with restored text + image+text fused multimodal search examples.
   - Restore 2a–2e fidelity to vendor (strip the early "Try it Yourself: Travel"
     sections; restore vendor body content; copy vendor `dataset/`,
     `dataset.test/`, `search_images/`; create `chroma_db/` placeholder).
   - Migrate Try-It-Yourself sandbox content into `99-try-it-yourself-travel.ipynb`
     as `From 2x` sub-sections and add cross-link pointers in each 2x notebook.
     Update paths from `assets/travel-*` to `try.travel/...`.
   - Add Mermaid visuals to the four key doc files.
   - Comments pass: add teaching comments to Lab 0/1 code cells, scripts, Python
     modules, and the capstone notebook. Skip 2a–2e (vendor).
   - Plain-language pass: rewrite markdown across Lab 0/1 + the capstone to
     high-school-friendly explanations. Skip 2a–2e markdown.
   - Separator pass: add `---` after `What you will do` in every notebook.
   - Combined Codespaces-first + Cohere-endpoint-reconciliation pass.

## 12. Validation checklist (run before declaring done)

```bash
cd cohere

# 1. notebooks parse as JSON
find . -name "*.ipynb" -exec python -c "import json,sys; json.load(open(sys.argv[1]))" {} \;

# 2. python AST clean
python -c "import ast; [ast.parse(open(p).read()) for p in [
  'lab-1-foundry-agent/_tracing.py',
  'lab-1-foundry-agent/tools/booking_tools.py',
  'lab-1-foundry-agent/evaluators/policy_adherence.py',
  'lab-1-foundry-agent/09-load-test.py',
]]"

# 3. bash scripts parse
bash -n lab-0-setup/provision.sh lab-0-setup/setup-env.sh lab-0-setup/verify.sh

# 4. Bicep builds
az bicep build --file lab-0-setup/bicep/main.bicep --stdout > /dev/null

# 5. terminology
! grep -rn "Azure AI Foundry" .

# 6. every env var used in code is defined in sample.env
USED=$(grep -rhoE 'os\.(environ|getenv)[^)]*"[A-Z][A-Z0-9_]+"' . | grep -oE '"[A-Z][A-Z0-9_]+"' | tr -d '"' | sort -u)
for v in $USED; do grep -q "^${v}=" sample.env || echo "MISSING: $v"; done

# 7. requirements install cleanly
pip install --dry-run -r requirements.txt

# 8. every notebook has Scenario, What you will do, What you learned (3 takeaways)
# (script: parse each notebook, check section titles and count "- " bullets in the final cell)
```

## 13. Reuse / re-implementation notes

- The plan deliberately separates **vendor reuse** (2a–2e under fidelity policy) from
  **bespoke content** (everything else). If the upstream Cohere notebooks evolve,
  re-pull them, re-apply only the five sanitization rules in §10/Lab 2, and add the
  workshop frame from §6. The capstone sandbox stays as-is.
- If you target a different Azure region, confirm all four model deployments are
  available there before changing `AZURE_LOCATION` defaults — Cohere model
  availability is the binding constraint.
- If the new Foundry SDK changes the prompt-agent API shape, notebooks 02 and 08 may
  need updates. The pattern to preserve: create agent, attach tools, attach
  grounding, persist `LAB1_AGENT_ID`. For 08, the AI Red Teaming agent SDK is the
  most volatile — keep the graceful-degradation fallback.
- If using a pre-provisioned RG, learners can skip `provision.sh` entirely. The
  `setup-env.sh` script discovers everything by querying the RG; deployment
  lookups are by `properties.model.name` (not deployment name) so casing
  differences don't break the workflow.

---

## 12. Backlog — navigability improvements (post-implementation review)

Generated 2026-06-08 from a self-guided-learner walkthrough of the repo. Tackle high-impact first; medium- and low-impact are nice-to-haves before broader release.

### High-impact
- [x] **H1.** Add `**Next →** / **← Workshop home**` footer to every lab README and SETUP doc so each file is a self-contained navigation node.
- [x] **H2.** Add "Open in Codespaces" badge to the very top of both the root `README.md` and `cohere/README.md` to collapse activation into one click.
- [x] **H3.** Stub `anthropic/README.md` and `foundry/README.md` with a "coming soon — see Cohere workshop for the pattern" note so the linked folders are not visibly empty.
- [x] **H4.** Fix the time badge on `cohere/lab-1-foundry-agent/README.md` (currently `> ⏱ ~3 hours`, should reflect the ~90 min budget).

### Medium-impact (consistency)
- [x] **M1.** Unify the three lab README templates (Scenario → What you will do → Notebooks → Supporting files → What you learned → Next).
- [x] **M2.** Add a consistent time badge + "How to run this lab" stanza (run order, prerequisites) to every lab README.
- [x] **M3.** Rename `99-try-it-yourself-travel.ipynb` → `2f-capstone-try-it-yourself-travel.ipynb`; update README/links.
- [x] **M4.** Promote `lab-1-foundry-agent/bridge/travel-capability-hooks.md` into both the Lab 1 and Lab 2 READMEs as a first-class link.

### Low-impact (polish)
- [x] **L1.** Add a one-line "If you only have 5 minutes" stanza to the root README.
- [ ] **L2.** Add a `GLOSSARY.md` (or "Vocabulary" section in the workshop README) so advanced learners can skip the per-notebook re-definitions.
- [x] **L3.** Surface `sample.env` from the root README ("every workshop has a sample.env documenting all required vars").
- [x] **L4.** Add a "you are here" Mermaid diagram to each lab README highlighting the current lab in the workshop arc.