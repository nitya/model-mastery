name: product-launch-copywriter-grounding
agent:
  name: product-launch-studio
  kind: hosted
  model: copywriter-fixed
  config: .agent_configs/baseline/metadata.yaml
dataset:
  local_uri: eval-data/copywriter-grounding.jsonl
evaluators:
  - task_adherence
  - groundedness
target_attributes:
  - instructions
options:
  eval_model: evaluator-model
  optimization_model: GPT-5.4
  max_candidates: 4
